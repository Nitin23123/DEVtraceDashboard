---
phase: 06-devops
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DEVOPS-02

must_haves:
  truths:
    - "Anyone can clone the repo and run the app with one command: docker compose up"
    - "Prerequisites (Docker Desktop, Git, .env file) are clearly listed before the quickstart"
    - "README explains what the app is and lists all major features"
    - "No secrets or credentials appear anywhere in the README"
  artifacts:
    - path: "README.md"
      provides: "Project documentation and quickstart guide"
      contains: "docker compose up"
  key_links:
    - from: "README.md"
      to: ".env.example"
      via: "cp .env.example .env instruction in setup steps"
      pattern: "env.example"
    - from: "README.md"
      to: "docker-compose.yml"
      via: "docker compose up quickstart command"
      pattern: "docker compose up"
---

<objective>
Write a README.md that lets any developer clone the repo and run the full stack with `docker compose up` — no manual steps beyond copying `.env.example` to `.env`.

Purpose: Interview-readiness and onboarding. Anyone looking at the repo (recruiter, interviewer, collaborator) should understand the project and get it running in under 5 minutes.
Output: README.md with quickstart, prerequisites, feature list, tech stack table, and architecture overview.
</objective>

<execution_context>
@C:/Users/nitin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nitin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docker-compose.yml
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README with quickstart and project overview</name>
  <files>README.md</files>
  <action>
    Create `README.md` in the repo root with the following sections in order. Write real, specific content — not placeholders.

    ---

    **Title:** `# DevTrackr`

    **Tagline:** One-line description. Example: "A full-stack developer productivity app — track tasks, notes, goals, and API requests while showing off your GitHub activity."

    ---

    **## Features**

    Bullet list of all features (pulled from STATE.md decisions and phase summaries):
    - Task management with status (todo / in_progress / done), priority, and due dates
    - Notes with create/edit/delete
    - Goals with completion tracking
    - Built-in API Tester (send HTTP requests, view response, browse history)
    - Dashboard with task stats, streak counter, and contribution heatmap
    - GitHub OAuth integration — connect your account to show profile stats and PR queue
    - Pomodoro timer
    - Dark mode (CSS variable-based theming, persisted in localStorage)
    - Code snippets manager
    - DSA problem sheet tracker (79 curated problems)
    - Task pinning
    - Smooth Framer Motion page transitions and modal animations

    ---

    **## Tech Stack**

    Markdown table with three columns: Layer | Technology | Notes

    Rows:
    | Frontend | React 18 (CRA) | Tailwind v3, Framer Motion, Lenis smooth scroll |
    | Backend | Node.js / Express | JWT auth, node-fetch@2 for proxy |
    | Database | PostgreSQL 15 | pg library, raw SQL migrations |
    | Auth | JWT (httpOnly-style) | Tokens in localStorage, bcrypt salt=12 |
    | GitHub | OAuth 2.0 | State param carries JWT as base64 |
    | DevOps | Docker Compose | 3 containers: frontend, backend, postgres |
    | CI | GitHub Actions | Build check on push to main |

    ---

    **## Prerequisites**

    Numbered list:
    1. Docker Desktop installed and running (https://docs.docker.com/get-docker/)
    2. Git

    Optional (only needed for GitHub OAuth feature):
    - A GitHub OAuth App: create at https://github.com/settings/developers → OAuth Apps
      - Homepage URL: `http://localhost:3000`
      - Callback URL: `http://localhost:5000/api/profile/github/callback`

    ---

    **## Quickstart**

    Use a code block with shell commands:

    ```bash
    # 1. Clone the repo
    git clone https://github.com/your-username/devtrace.git
    cd devtrace

    # 2. Copy environment variables
    cp .env.example .env
    # Edit .env if you want GitHub OAuth — otherwise leave as-is

    # 3. Start all services
    docker compose up
    ```

    Then: "Open http://localhost:3000 in your browser."

    Add a note: "The first run downloads base images and installs dependencies — allow 2-3 minutes. Subsequent starts are fast."

    ---

    **## Environment Variables**

    Explain that all config lives in `.env` (gitignored). Reference `.env.example` as the template.

    Table of variables:

    | Variable | Required | Description |
    |----------|----------|-------------|
    | POSTGRES_DB | Yes | Database name |
    | POSTGRES_USER | Yes | Database user |
    | POSTGRES_PASSWORD | Yes | Database password |
    | JWT_SECRET | Yes | Secret for signing JWT tokens — use a long random string in production |
    | JWT_EXPIRES_IN | Yes | Token lifetime (e.g. `24h`) |
    | DATABASE_URL | Yes | Full PostgreSQL connection string |
    | GITHUB_CLIENT_ID | Optional | GitHub OAuth App client ID |
    | GITHUB_CLIENT_SECRET | Optional | GitHub OAuth App client secret |
    | REACT_APP_API_URL | Yes | Backend URL visible from the browser |

    Add: "Never commit `.env` — it is gitignored. `.env.example` contains safe placeholder values."

    ---

    **## Architecture**

    ASCII diagram showing the three Docker Compose services and how they connect:

    ```
    Browser
       |
       v
    frontend (React, :3000)
       |
       v  (HTTP API calls)
    backend (Express, :5000)
       |
       v
    postgres (PostgreSQL, :5432)
    ```

    One-paragraph explanation: The frontend is a React CRA app served by react-scripts. The backend is an Express API that handles all business logic, authentication, and database access. PostgreSQL stores all user data. All three run as Docker containers orchestrated by Docker Compose.

    ---

    **## CI**

    One paragraph: "A GitHub Actions workflow runs on every push to `main`. It builds both Docker images to verify the project compiles without errors. See `.github/workflows/ci.yml`."

    ---

    **## License**

    `MIT`

    ---

    IMPORTANT RULES for writing the README:
    - Do NOT include any actual secret values anywhere (no real JWT secrets, no real OAuth credentials)
    - Do NOT include any passwords — use "changeme" only as shown in .env.example
    - Keep prose concise — developers scan READMEs, they don't read them
    - The quickstart section must be self-contained: someone should copy those 3 commands and be running the app
  </action>
  <verify>
    <automated>grep -c "docker compose up" /c/Users/nitin/Desktop/project/devtrace/README.md</automated>
  </verify>
  <done>
    `README.md` exists at repo root. Contains: project title, features list, tech stack table, prerequisites (Docker Desktop), quickstart with `cp .env.example .env` and `docker compose up`, environment variables table referencing `.env.example`, architecture ASCII diagram, CI section. No hardcoded secrets anywhere in the file.
  </done>
</task>

</tasks>

<verification>
- `README.md` exists at repo root
- Contains `docker compose up` in a code block
- Contains `cp .env.example .env` instruction
- Contains prerequisites section mentioning Docker Desktop
- Contains tech stack table
- Contains features list covering the 12+ features built across phases
- Does NOT contain any actual secret values
- grep for "JWT_SECRET=change" or similar placeholder confirms no real secrets
</verification>

<success_criteria>
A new developer cloning the repo can read the README, copy `.env.example` to `.env`, run `docker compose up`, and reach the app at `http://localhost:3000`. The README explains what the app does and how it's built.
</success_criteria>

<output>
After completion, create `.planning/phases/06-devops/06-devops-02-SUMMARY.md` summarizing what was written in the README and confirming no secrets are present.
</output>
