---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - frontend/Dockerfile
  - backend/Dockerfile
  - .env.example
  - .env
autonomous: true
requirements:
  - DEVOPS-01
  - DEVOPS-03

must_haves:
  truths:
    - "`docker compose up` starts all 3 containers (frontend, backend, postgres) without errors"
    - "Frontend container is reachable at http://localhost:3000"
    - "Backend container is reachable at http://localhost:5000/health"
    - "PostgreSQL container is running and accepting connections on port 5432"
    - "All config (DB credentials, JWT secret, ports) comes from .env, not hardcoded"
  artifacts:
    - path: "docker-compose.yml"
      provides: "3-service orchestration (frontend, backend, postgres)"
      contains: "services: frontend, backend, postgres"
    - path: "frontend/Dockerfile"
      provides: "React development container"
      contains: "npm start"
    - path: "backend/Dockerfile"
      provides: "Express development container"
      contains: "npm run dev"
    - path: ".env.example"
      provides: "Reference for all required environment variables"
      contains: "POSTGRES_USER, JWT_SECRET"
    - path: ".env"
      provides: "Actual environment variable values (gitignored)"
  key_links:
    - from: "docker-compose.yml"
      to: ".env"
      via: "env_file directive"
      pattern: "env_file"
    - from: "backend/Dockerfile"
      to: "backend/src"
      via: "COPY and CMD"
      pattern: "COPY.*src"
    - from: "docker-compose.yml postgres service"
      to: "backend depends_on"
      via: "depends_on with healthcheck condition"
      pattern: "depends_on.*postgres"
---

<objective>
Create the Docker Compose environment that starts all 3 containers with a single `docker compose up` command. Environment variables are loaded from .env files — no credentials hardcoded anywhere.

Purpose: Without this, nothing runs. Every subsequent plan depends on containers being reachable.
Output: docker-compose.yml, frontend/Dockerfile, backend/Dockerfile, .env.example, .env
</objective>

<execution_context>
@C:/Users/nitin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nitin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dockerfiles for frontend and backend</name>
  <files>frontend/Dockerfile, backend/Dockerfile</files>
  <action>
Create `frontend/Dockerfile`:
```dockerfile
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
EXPOSE 3000
CMD ["npm", "start"]
```

Create `backend/Dockerfile`:
```dockerfile
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
EXPOSE 5000
CMD ["npm", "run", "dev"]
```

Both use node:20-alpine for minimal image size. Frontend runs on port 3000 (React default), backend on port 5000.
  </action>
  <verify>Both files exist: `ls frontend/Dockerfile backend/Dockerfile`</verify>
  <done>frontend/Dockerfile and backend/Dockerfile exist with correct port EXPOSE and CMD directives</done>
</task>

<task type="auto">
  <name>Task 2: Create docker-compose.yml and .env files</name>
  <files>docker-compose.yml, .env.example, .env, .gitignore</files>
  <action>
Create `docker-compose.yml` at the repo root:
```yaml
version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    restart: unless-stopped
    env_file:
      - .env
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/src/db/migrations:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "5000:5000"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./backend:/app
      - /app/node_modules

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    depends_on:
      - backend
    environment:
      - REACT_APP_API_URL=http://localhost:5000
    volumes:
      - ./frontend:/app
      - /app/node_modules
    stdin_open: true

volumes:
  postgres_data:
```

Create `.env.example` (committed to git — no real secrets):
```
# Database
POSTGRES_DB=devtrackr
POSTGRES_USER=devtrackr_user
POSTGRES_PASSWORD=changeme

# Backend
NODE_ENV=development
PORT=5000
JWT_SECRET=change-this-to-a-long-random-string-in-production
JWT_EXPIRES_IN=24h

# Database URL (used by backend)
DATABASE_URL=postgresql://devtrackr_user:changeme@postgres:5432/devtrackr

# Frontend
REACT_APP_API_URL=http://localhost:5000
```

Create `.env` (same content as .env.example for local dev — this file is gitignored):
```
# Database
POSTGRES_DB=devtrackr
POSTGRES_USER=devtrackr_user
POSTGRES_PASSWORD=devtrackr_pass_local

# Backend
NODE_ENV=development
PORT=5000
JWT_SECRET=dev-jwt-secret-not-for-production-use-only
JWT_EXPIRES_IN=24h

# Database URL (used by backend)
DATABASE_URL=postgresql://devtrackr_user:devtrackr_pass_local@postgres:5432/devtrackr

# Frontend
REACT_APP_API_URL=http://localhost:5000
```

Ensure `.gitignore` at root includes `.env` but NOT `.env.example`. Create or append:
```
# Environment variables
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# Build outputs
frontend/build/
backend/dist/
```
  </action>
  <verify>Run `docker compose config` — it should print the resolved configuration with no errors. Confirm `cat .env.example` shows all required vars.</verify>
  <done>docker-compose.yml passes `docker compose config` validation. .env.example committed. .env exists but is gitignored. All 3 services defined with correct ports and healthcheck on postgres.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `docker compose config` — resolves without errors
2. `docker compose up -d --build` — all 3 containers reach running state
3. `docker compose ps` — STATUS shows "Up" for all 3 services
4. `curl http://localhost:5000/health` — NOTE: backend /health route is created in plan 01-03; at this stage the backend container will start but return connection refused until the app is scaffolded. Verify only that the container starts.
5. `cat .env` shows real values, `git status` shows .env is untracked (gitignored)
</verification>

<success_criteria>
- `docker compose up -d` runs without errors
- `docker compose ps` shows 3 containers in running state
- PostgreSQL healthcheck passes (backend container starts successfully after it)
- .env is gitignored, .env.example is tracked by git
- No credentials hardcoded in any committed file
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` with:
- What was built (files created, key decisions)
- Docker service names and ports used
- Environment variable names defined
- Any deviations from the plan
</output>
